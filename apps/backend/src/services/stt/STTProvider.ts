/**\n * Speech-to-Text (STT) Provider abstraction layer\n * Allows swapping STT providers without changing application code\n */\n\nexport interface STTConfig {\n  language?: string;\n  sampleRate?: number;\n  encoding?: string;\n}\n\nexport interface STTResult {\n  transcript: string;\n  confidence: number; // 0-1\n  isFinal: boolean;\n  duration: number; // milliseconds\n}\n\nexport interface STTProvider {\n  /**\n   * Transcribe audio buffer to text\n   */\n  transcribe(audioBuffer: Buffer, config?: STTConfig): Promise<STTResult>;\n\n  /**\n   * Stream audio and get real-time transcription\n   */\n  streamTranscribe(\n    audioStream: NodeJS.ReadableStream,\n    config?: STTConfig,\n    onResult?: (result: STTResult) => void\n  ): Promise<STTResult>;\n\n  /**\n   * Health check\n   */\n  healthCheck(): Promise<boolean>;\n\n  /**\n   * Get supported languages\n   */\n  getSupportedLanguages(): Promise<string[]>;\n}\n